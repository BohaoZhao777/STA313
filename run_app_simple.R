# ============================================
# Shiny 应用启动脚本（简化版，支持热更新）
# ============================================

# 加载必要的包
library(shiny)

# 启用自动重载（热更新）
options(shiny.autoreload = TRUE)

# 运行应用（支持热更新）
# 修改 app.R 文件后，刷新浏览器即可看到更新
runApp(
  port = 3838,
  host = "0.0.0.0",
  launch.browser = TRUE
)

